// Importa o 'gulp'
const gulp = require('gulp')

// Cria a task 'deps'
gulp.task('deps', ['deps.js', 'deps.css', 'deps.fonts'])

// Cria a task JS
gulp.task('deps.js', () => {

})

// Cria a task CSS
gulp.task('deps.css', () => {

})

// Cria a task FONTS
gulp.task('deps.fonts', () => {

})